legacy-peer-deps=true
# Flat, symlink-free node_modules. Required for Vercel: pnpm's default symlinked
# layout produces "invalid deployment package ... symlinked directories" errors
# when native deps (sharp/libvips) are traced into serverless functions.
node-linker=hoisted
